import AgeCalculator from '../components/AgeCalculator/AgeCalculator';
// import other variants if needed

export const toolCategoryMap = {
  ageCalculator: {
    General: AgeCalculator,
    // High: AgeCalculatorHigh,
    // Low: AgeCalculatorLow,
    // Global: AgeCalculatorGlobal,
  }
};